# ImageWatcher 프로토타입 개발 계획

## 1. 프로젝트 개요

ImageWatcher는 Windows 화면에서 사용자가 등록한 기준 이미지를 실시간으로 탐지하는 무(無) UI 프로그램이다. 사용자는 기준 이미지와 설정 파일을 준비한 뒤 `start.cmd`를 실행하는 것만으로 감시를 시작할 수 있어야 한다.

프로토타입의 핵심 목표는 다음과 같다.

- 설치와 사용 절차를 최대한 단순하게 만든다.
- 별도의 그래픽 UI 없이 CMD에서 즉시 실행한다.
- 전체 화면 또는 지정 영역에서 기준 이미지를 빠르게 찾는다.
- 감지 결과를 알림, 로그, 스크린샷으로 확인할 수 있게 한다.
- 처음 사용하는 사람도 `사용설명서.txt`만 읽고 실행할 수 있게 한다.

## 2. 사용자 실행 흐름

1. 찾으려는 이미지를 `reference.png`라는 이름으로 프로그램 폴더에 둔다.
2. 기본값을 바꾸려면 `settings.ini`를 편집한다.
3. `start.cmd`를 더블클릭한다.
4. CMD 창에서 설정과 실행 상태를 확인한다.
5. 이미지가 발견되면 알림을 받고 기록을 확인한다.
6. 종료할 때 CMD 창에서 `Ctrl+C`를 누른다.

기본 설정은 별도의 편집 없이 바로 실행할 수 있도록 전체 화면 감시로 제공한다.

## 3. 프로토타입 범위

### 포함 기능

- PNG/JPG 기준 이미지 한 개 등록
- 주 모니터 전체 또는 좌표로 지정한 영역 감시
- OpenCV 템플릿 매칭 기반 이미지 탐지
- 민감도(일치 임계값) 설정
- 검사 간격 설정
- 연속 프레임 확인을 통한 오탐 완화
- 같은 대상의 중복 알림 방지
- 감지 시 CMD 메시지 출력
- 감지 시 소리 또는 Windows 알림
- 감지 시각과 일치율 로그 저장
- 감지 순간의 스크린샷 저장
- 설정 파일이 없을 때 기본 파일 자동 생성
- 잘못된 설정과 누락 파일에 대한 명확한 오류 출력
- `Ctrl+C`를 통한 안전한 종료

### 프로토타입에서 제외할 기능

- 그래픽 사용자 인터페이스
- OCR 문자 인식
- 객체 탐지 AI 모델
- 여러 기준 이미지 동시 감시
- 자동 클릭이나 키보드 입력
- 원격 알림 및 서버 연동
- 영상 녹화
- 시스템 시작 시 자동 실행

제외 기능은 기본 탐지 기능의 정확도와 안정성을 검증한 뒤 확장한다.

## 4. 권장 기술 구성

- 언어: Python 3.12 계열
- 화면 캡처: `mss`
- 이미지 처리: `opencv-python`
- 배열 처리: `numpy`
- 설정: Python 표준 라이브러리 `configparser`
- 로그: Python 표준 라이브러리 `logging`
- 알림: Windows 알림 라이브러리 또는 기본 소리 기능
- 실행 파일 패키징: `PyInstaller`

프로토타입은 가볍고 검증이 쉬운 템플릿 매칭을 우선 사용한다. AI 모델은 필요성이 확인되기 전까지 도입하지 않는다.

## 5. 권장 파일 구조

```text
ScreanCapture_solo/
├─ src/
│  ├─ main.py
│  ├─ capture.py
│  ├─ detector.py
│  ├─ config.py
│  ├─ notifier.py
│  └─ event_logger.py
├─ tests/
│  ├─ test_config.py
│  └─ test_detector.py
├─ reference.png
├─ settings.ini
├─ start.cmd
├─ 사용설명서.txt
├─ requirements.txt
├─ StartPlan.md
├─ logs/
└─ captures/
```

배포본은 사용자가 내부 소스 코드를 몰라도 되도록 다음과 같이 단순화한다.

```text
ImageWatcher/
├─ ImageWatcher.exe
├─ reference.png
├─ settings.ini
├─ start.cmd
├─ 사용설명서.txt
├─ logs/
└─ captures/
```

## 6. 설정 파일 초안

```ini
[watch]
reference = reference.png
monitor = 1
region = full
confidence = 0.85
interval_ms = 100
consecutive_matches = 2
cooldown_seconds = 3

[notification]
sound = true
desktop_notification = true
save_capture = true

[storage]
log_directory = logs
capture_directory = captures
retention_days = 7
```

`region`의 기본값은 `full`이다. 지정 영역을 사용할 경우 `left,top,width,height` 형식으로 입력한다.

예시:

```ini
region = 100,200,800,600
```

모든 설정값은 실행 직후 검증한다. 범위를 벗어난 민감도, 음수 좌표, 존재하지 않는 기준 이미지 등은 감시를 시작하기 전에 오류로 알려야 한다.

## 7. CMD 출력 초안

```text
ImageWatcher Prototype

기준 이미지 : reference.png
감시 대상   : 모니터 1 / 전체 화면
민감도      : 85%
검사 속도   : 100ms

감시를 시작했습니다.
종료하려면 Ctrl+C를 누르세요.

[14:21:08] 이미지 발견 | 일치율 92% | 캡처 저장 완료
[14:21:11] 이미지 사라짐
```

오류로 종료되더라도 CMD 창이 즉시 닫히지 않도록 `start.cmd` 끝에 `pause`를 둔다.

```bat
@echo off
chcp 65001 > nul
ImageWatcher.exe --config settings.ini
pause
```

개발 단계에서는 실행 파일 대신 Python을 호출하는 별도 스크립트를 둘 수 있다.

## 8. 처리 구조

```text
설정 읽기 및 검증
        ↓
기준 이미지 로드
        ↓
화면 또는 지정 영역 캡처
        ↓
템플릿 매칭 및 최고 일치율 계산
        ↓
연속 감지 여부 확인
        ↓
상태 변경 판정
        ↓
CMD 출력 · 알림 · 로그 · 스크린샷
```

### 주요 모듈 책임

- `config.py`: INI 읽기, 기본 설정 생성, 값 검증
- `capture.py`: 모니터 정보 확인과 지정 영역 캡처
- `detector.py`: 기준 이미지 전처리와 템플릿 매칭
- `notifier.py`: 소리와 Windows 알림 처리
- `event_logger.py`: 이벤트 로그와 캡처 파일 저장
- `main.py`: 초기화, 감시 루프, 상태 전환, 안전한 종료

## 9. 성능 및 안정성 전략

- 기본 검사 간격은 100ms로 두어 초당 약 10회 검사한다.
- 전체 화면보다 지정 영역 감시를 권장한다.
- 이전 처리가 끝나지 않았으면 오래된 프레임을 쌓지 않고 최신 화면을 사용한다.
- 2회 연속 임계값을 넘을 때 발견으로 확정한다.
- 감지 후 기본 3초 동안 같은 대상의 알림을 반복하지 않는다.
- 대상이 사라졌다고 판단한 뒤 다시 나타나면 새로운 이벤트로 기록한다.
- 영상 전체는 저장하지 않고 감지 순간의 이미지 만 저장한다.
- 로그와 캡처 파일은 보존 기간을 기준으로 정리한다.
- 사용자 화면에 개인정보가 포함될 수 있으므로 저장 여부를 설정에서 끌 수 있게 한다.

## 10. 명령행 옵션

최소한 다음 옵션을 지원한다.

```text
ImageWatcher.exe --config settings.ini
ImageWatcher.exe --test
ImageWatcher.exe --list-monitors
ImageWatcher.exe --help
```

- `--config`: 사용할 설정 파일 지정
- `--test`: 화면을 한 번만 검사하고 일치율을 출력
- `--list-monitors`: 사용할 수 있는 모니터 번호와 해상도 출력
- `--help`: 명령과 설정 방법 요약 출력

`--test`는 기준 이미지와 민감도가 적절한지 본 실행 전에 검증하는 용도로 사용한다.

## 11. 개발 단계

### 1단계: 실행 골격

- 프로젝트 구조 생성
- 의존성 정의
- INI 설정 읽기와 검증
- CMD 상태 및 오류 출력
- `Ctrl+C` 안전 종료

### 2단계: 탐지 기능

- `mss` 화면 캡처
- 기준 이미지 로드
- OpenCV 템플릿 매칭
- 최고 일치율과 위치 출력
- `--test` 구현

### 3단계: 실시간 감시

- 주기적 캡처 및 분석 루프
- 연속 감지와 사라짐 판정
- 중복 알림 방지
- 처리 속도와 CPU 사용량 측정

### 4단계: 기록과 알림

- 감지 로그 저장
- 감지 화면 저장
- 소리와 Windows 알림
- 저장 실패가 감시 루프를 중단하지 않도록 예외 처리

### 5단계: 배포 준비

- `start.cmd` 작성
- `사용설명서.txt` 작성
- PyInstaller 단일 실행 파일 생성
- 소스 없는 새 폴더에서 실제 실행 검증

## 12. 테스트 계획

다음 조건을 직접 실행해 검증한다.

- 기준 이미지가 화면에 있을 때 정상 감지
- 기준 이미지가 없을 때 알림 미발생
- 이미지가 나타났다 사라지는 상태 전환
- 동일 이미지가 계속 표시될 때 중복 알림 제한
- 전체 화면과 지정 영역 각각의 동작
- 잘못된 영역 좌표와 임계값 처리
- 기준 이미지 누락 또는 손상 처리
- Windows 화면 배율 100%, 125%, 150% 환경
- 다중 모니터 환경과 음수 좌표
- 한글 경로에서 로그와 캡처 저장
- 장시간 실행 시 메모리 증가 여부
- PyInstaller 배포본을 개발 환경이 아닌 PC에서 실행

초기 성공 기준은 다음과 같이 잡는다.

- 테스트 이미지 기준 감지율 95% 이상
- 일반적인 지정 영역에서 평균 감지 지연 300ms 이하
- 대상이 없는 상태에서 오탐이 반복되지 않을 것
- 2시간 연속 실행 중 비정상 종료가 없을 것
- 설정 오류가 발생했을 때 원인과 해결 방법이 CMD에 표시될 것

성능 수치는 대상 이미지, 해상도, 하드웨어에 따라 달라지므로 실제 테스트 결과를 측정해 확정한다.

## 13. 알려진 위험과 대응

- 화면 배율이나 이미지 크기가 달라지면 템플릿 매칭 정확도가 떨어질 수 있다.
  - 프로토타입 검증 후 90%, 100%, 110% 다중 크기 비교를 추가한다.
- 애니메이션, 반투명 효과, 밝기 변화로 오탐이 발생할 수 있다.
  - 연속 프레임 확인과 민감도 조절로 대응한다.
- 전체 화면을 빠르게 검사하면 CPU 사용량이 증가할 수 있다.
  - 지정 영역 사용을 권장하고 검사 간격을 설정 가능하게 한다.
- DRM 또는 일부 GPU 가속 화면은 일반 캡처가 제한될 수 있다.
  - 지원되지 않는 화면임을 문서에 명시하고 실제 대상 프로그램에서 사전 검증한다.
- 스크린샷에 민감한 정보가 저장될 수 있다.
  - 기본 보존 기간을 두고 저장 기능을 비활성화할 수 있게 한다.

## 14. 완료 조건

프로토타입은 다음 조건을 모두 만족하면 완료로 판단한다.

- 새 사용자가 `사용설명서.txt`를 읽고 1분 안에 감시를 시작할 수 있다.
- `start.cmd`를 더블클릭하면 별도 UI 없이 감시가 시작된다.
- 이미지 발견과 사라짐이 CMD에 명확히 표시된다.
- 알림, 로그, 스크린샷 저장이 설정대로 동작한다.
- 오류가 발생해도 원인을 확인할 수 있고 CMD 창이 즉시 닫히지 않는다.
- 패키징된 프로그램이 Python 미설치 환경에서도 실행된다.

## 15. 프로토타입 이후 확장 우선순위

1. 여러 기준 이미지 동시 감시
2. 화면에서 기준 이미지와 감시 영역을 캡처하는 보조 도구
3. 크기 변화에 강한 다중 스케일 탐지
4. OCR 기반 문자·숫자 감시
5. 시스템 트레이와 백그라운드 실행
6. 외부 알림 연동

확장 기능은 실제 사용 중 확인된 불편과 오탐·미탐 데이터를 기준으로 결정한다.
